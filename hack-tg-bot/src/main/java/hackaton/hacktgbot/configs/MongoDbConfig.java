package hackaton.hacktgbot.configs;

/*import com.mongodb.ConnectionString;
import com.mongodb.MongoClientSettings;
import com.mongodb.client.MongoClient;
import com.mongodb.client.MongoClients;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.mongodb.core.MongoTemplate;
import org.springframework.data.mongodb.repository.config.EnableMongoRepositories;*/


//@EnableMongoRepositories(basePackages = "hackaton.hacktgbot.repositories")
//@Configuration
public class MongoDbConfig {

    /*@Bean
    public MongoClient mongoClient() {
        /*ConnectionString connectionString = new ConnectionString("mongodb://localhost:27017/tgbot");
        MongoClientSettings mongoClientSettings = MongoClientSettings.builder()
                .applyConnectionString(connectionString)
                .build();*
        return MongoClients.create("mongodb://localhost:27017/tgbot");
    }

    @Bean
    public MongoTemplate mongoTemplate() throws Exception {
        return new MongoTemplate(mongoClient(), "tgbot");
    }*/
}
